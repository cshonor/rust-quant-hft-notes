## 3. 基于 DPDK 的网络存储优化思路

> **全栈用户态：** 从 **网络前端** 到 **存储后端** 消除内核与多余拷贝。

---

### 一、总体架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户态（SPDK 栈）                      │
│  PMD 收包 → 用户态 TCP/IP → iSCSI 解析 → 用户态 NVMe 驱动 │
│       ↑           ↑              ↑              ↑        │
│     DPDK        L4 栈         存储协议        轮询 NVMe   │
└─────────────────────────────────────────────────────────┘
         │                                      │
    物理网卡                               NVMe SSD
    （绕过内核 net stack）                  （绕过内核 block 层）
```

**目标：** 网卡到落盘 **全程用户空间轮询**，逼近 **零拷贝**。

---

### 二、网络前端优化

| 手段 | 说明 |
|------|------|
| **DPDK PMD** | 轮询收包 — 无中断、无内核 netdevice |
| **用户态 TCP/IP** | 绕过内核协议栈 — 无 syscall、**无栈内锁** |
| **独立内存池 / 队列** | per-core 资源 — [Ch2 mempool](../chapter-02-cache-and-memory/) · [Ch4 ring](../chapter-04-synchronization/) |

**DPDK 边界：** 原生 DPDK 处理 **OSI L2/L3** — **不含 TCP/UDP**；存储场景必须 **叠加 L4 用户态栈**（见 [4. SPDK](./section-4-SPDK架构与核心组件.md)）。

---

### 三、存储后端优化

| 手段 | 说明 |
|------|------|
| **用户态 NVMe 驱动** | 直接映射 PCIe BAR、轮询 completion queue |
| **摒弃中断与内核锁** | 与 PMD 同构 — **绑核 + 无锁 I/O 队列** |
| **队列亲和** | DPDK 线程与 NVMe **I/O Queue Pair** 一一绑定 |

→ [Ch6 PCIe 与 DMA](../chapter-06-pcie-packet-io/) — NVMe 同为 **PCIe 设备**

---

### 四、数据流（理想路径）

```
NIC ──PMD──► mbuf 池 ──► 用户态 TCP ──► iSCSI Target 逻辑
                                              │
                                              ▼
                                         NVMe 提交队列
                                              │
                                              ▼
                                         SSD 落盘
```

| 消除项 | 效果 |
|--------|------|
| 内核网络拷贝 | ↓ 延迟、↑ 吞吐 |
| 内核存储拷贝 | ↓ CPU 占用 |
| 上下文切换 | 稳态 **纯轮询** |

---

← [2. 传统瓶颈](./section-2-传统以太网存储瓶颈.md) · 下一节 [4. SPDK 架构](./section-4-SPDK架构与核心组件.md)
