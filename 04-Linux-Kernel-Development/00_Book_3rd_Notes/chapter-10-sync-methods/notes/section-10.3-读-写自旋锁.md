## ③ 读-写自旋锁 · Reader-Writer Spin Locks

使用模式能清晰分为 **读路径 / 写路径** 时用 **rwlock**。

| 锁模式 | 规则 |
|--------|------|
| **读锁** | **多个读者** 可同时持有 |
| **写锁** | **唯一写者** — 且 **无任何读者** |

| 注意 | 说明 |
|------|------|
| Linux rwlock **偏袒读者** | 大量读者可能导致 **写者饥饿** |

| 场景 | 选型 |
|------|------|
| 读 ≫ 写、可容忍写延迟 | rwlock |
| 写不能饿死 | 考虑 **seqlock**（⑧） |

---
