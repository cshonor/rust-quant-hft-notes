## ⑤ 互斥体 · Mutexes

比「计数为 1 的信号量」更现代的 **睡眠互斥** — **`struct mutex`**。

| 对比信号量 | mutex |
|------------|-------|
| 接口 | **更简单** |
| 性能 | **更高**（典型路径） |
| 规则 | **更严格** + 调试器可自动检查 |

#### 严格规则

| 规则 | 说明 |
|------|------|
| 仅 **一个** 任务持有 | |
| **谁 lock 谁 unlock** | |
| **不可递归** | |
| **不能** 在中断或下半部获取 | softirq/tasklet 也不行 |

#### 作者建议

> **新代码应始终首选 mutex**，除非限制阻碍你（如需计数 >1、需在中断用 — 那时用 spinlock/原子）。

**HFT 用户态类比：** `std::mutex` 规则类似 — 热路径仍倾向 **无锁 / 单生产者单消费者**。

---
