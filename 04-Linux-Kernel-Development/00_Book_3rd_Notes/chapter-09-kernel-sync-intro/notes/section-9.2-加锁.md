## ② 加锁 · Locking

多指令、复杂结构 — 靠 **锁** 保证临界区 **同一时刻只有一个线程** 进入。

| 比喻 | 说明 |
|------|------|
| **房间门锁** | 一次一人进房改共享数据 |

#### 锁争用时的两种表现

| 类型 | 行为 | 典型场景（Ch 10） |
|------|------|-------------------|
| **忙等（自旋）** | 线程 **旋转等待** 锁释放 | 临界区 **极短**、已持有锁者在另一 CPU 跑着 |
| **睡眠** | 当前任务 **阻塞**，让出 CPU | 临界区 **较长**、可能睡眠 |

#### 建议性与自愿性

| 事实 | 含义 |
|------|------|
| 锁是 **advisory & voluntary** | **编程约定** — 内核不会替你自动加锁 |
| 程序员责任 | **主动** `lock` / `unlock` 保护 **数据** |

> 错误：只给某条代码路径加锁，另一条仍裸访共享数据 → 锁 **无效**。

---
