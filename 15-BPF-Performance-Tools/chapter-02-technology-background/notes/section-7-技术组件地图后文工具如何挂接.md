# 7. 技术组件地图（后文工具如何挂接）

```
                    ┌─────────────────────────────────┐
                    │  eBPF VM + 验证器 + Map + Helper │
                    └───────────────┬─────────────────┘
                                    │
     ┌──────────────┬───────────────┼───────────────┬──────────────┐
     ▼              ▼               ▼               ▼              ▼
 kprobes       uprobes        Tracepoints         USDT      perf_event/PMC
 (内核动态)    (用户动态)      (内核静态)        (用户静态)    (硬件采样)
     │              │               │               │              │
     └──────────────┴─────── BCC / bpftrace 工具 ──┴──────────────┘
                                    │
                          聚合 map / 栈 ID → 用户态展示
                                    │
                          火焰图 / 直方图 / 文本流
```

---
