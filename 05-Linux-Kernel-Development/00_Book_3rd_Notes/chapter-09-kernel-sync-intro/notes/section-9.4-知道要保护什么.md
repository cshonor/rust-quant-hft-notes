## ④ 知道要保护什么 · Knowing What to Protect

| 数据 | 是否需要锁 |
|------|-------------|
| **局部自动变量** | **通常否** — 各线程独立栈 |
| **全局 / 共享内核结构** | **几乎都要** |

#### 基本原则

| 规则 | 说明 |
|------|------|
| 其他执行线程 **能访问** 的数据 | **需要同步** |
| 名言 | **「保护数据，而不是保护代码」**（**lock data, not code**） |

| 反例（保护代码） | 正例（保护数据） |
|------------------|------------------|
| 「给整个函数加一把大锁」 | 明确 **哪些字段** 是共享的，用 **合适粒度的锁** 护住 **那些字段的读写** |

#### per-CPU / 无锁（预告）

| 手段 | 思路 |
|------|------|
| **per-CPU 变量** | 每 CPU 一份 — 减少共享（Ch 8 softirq） |
| **RCU** | 读多写少（**Ch 10**） |

---
