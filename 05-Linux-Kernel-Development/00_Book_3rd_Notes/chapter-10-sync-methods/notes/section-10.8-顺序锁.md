## ⑧ 顺序锁 · Sequential Locks · seqlock

2.6 引入 — 用 **序列计数器** 协调读写。

| 写者 | 读者 |
|------|------|
| 获取锁，写数据，**递增序列号**（变奇数表示写入中） | **读前读序列号 s1** → 读数据 → **读后再读 s2** |
| | 若 **s1 == s2 且为偶数** → 读期间 **无写入**，数据一致 |

| 特点 | 说明 |
|------|------|
| **偏袒写入者** | 读者可重试，**写入者不会被读者饿死** |
| 适合 | **读者极多、写者极少** |

| 例子 | **`jiffies`** 更新 |

| vs rwlock | seqlock 写者优先；rwlock 读者优先 |

---
