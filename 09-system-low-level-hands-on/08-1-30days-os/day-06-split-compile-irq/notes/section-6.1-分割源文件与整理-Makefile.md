## ① 分割源文件与整理 Makefile

`bootpack.c` 过长 → 按 **功能拆文件**：

| 文件（示例） | 职责 |
|--------------|------|
| **`graphic.c`** | 图形绘制 |
| **`dsctbl.c`** | **GDT / IDT** 等描述符表 |
| … | 其他模块（随原书扩展） |

**Makefile** 引入 **一般规则（pattern rule）**，把重复的「`.c` → `.o`」合成一条：

```makefile
# 示意
%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@
```

| 之前 | 之后 |
|------|------|
| 每个 `.c` 手写一条规则 | **一条模式规则** 覆盖多数源文件 |
| Makefile 臃肿 | **少数十行**，增删模块只改 **OBJS** |

**HFT：** 网关/引擎 **按模块分目录 + CMake/Make** — 同一工程卫生；热路径文件单独编译选项（`-O3`）也可挂在此层。

---
