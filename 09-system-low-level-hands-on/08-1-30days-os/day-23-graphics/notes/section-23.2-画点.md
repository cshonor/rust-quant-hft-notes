## ② 画点 · 控制窗口刷新

| EDX | 功能 |
|-----|------|
| **11** | 在 **窗口句柄 + (x,y)** **画点** |

#### 性能陷阱

**每点自动 `sheet_refresh`** → **`stars.hrb` 满天星** 极慢。

#### 解法

| 机制 | 说明 |
|------|------|
| **句柄最低位标志** | 置位 → **画点不自动刷新** |
| **EDX=12** | **手动局部 refresh** — 一批点画完 **一次刷** |

```
for (每颗星) api_point(..., no_auto_refresh);
api_refresh(win);   /* 一次更新屏幕 */
```

→ [Day 10–11 局部 refresh / map](../day-10-layers/)

---
