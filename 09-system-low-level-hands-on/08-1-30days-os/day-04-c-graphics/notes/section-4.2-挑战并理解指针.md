## ② 挑战并理解指针

C 写内存 = 必须告诉编译器 **地址** 和 **操作宽度**。

| 汇编 | C |
|------|---|
| `MOV BYTE [xxx], al` | **`char *p`** → **`*p = …`**（1 字节） |
| 明确 BYTE/WORD | **指针类型 + cast** |

```c
char *p = (char *) 0xa0000;   /* VRAM 起点 */
*p = 15;                      /* 写 1 字节色号 */
```

- **`*p`** ≈ 汇编 **内存寻址 `[...]`**
- **指针** = 「存地址的变量 + 元素大小由类型决定」

→ 对照 [01-CSAPP Ch3 指针与数组](../../../../01-CSAPP-3rd/chapter-03-machine-level-programs/)

---
