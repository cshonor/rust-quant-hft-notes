## ③ 高速计数器 · 初步消闪烁

#### 动态压测

- 窗口内 **计数器** 不断 **+1 并重绘数字**
- **去掉 HLT** — CPU **满速跑**（测 **refresh 性能**）

#### 闪烁原因（初版）

每次 **局部 refresh** 都从 **最底背景层** 开始 **整栈合成** → 计数器区域 **背景也被反复重画** → **闪**。

#### 初步优化

**只 refresh：**

1. **内容发生变化的 sheet**（计数器所在层）
2. **以及 Z 轴在其之上的所有 sheet**（如鼠标）

**不再从背景层重画整个 dirty 区** → **背景闪烁消失**。

```
优化前：背景 → … → 计数器 → 鼠标（全栈）
优化后：     … → 计数器 → 鼠标（仅变化层及以上）
```

---
