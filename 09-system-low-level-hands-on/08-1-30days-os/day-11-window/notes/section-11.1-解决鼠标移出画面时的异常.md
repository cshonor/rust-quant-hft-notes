## ① 解决鼠标移出画面时的异常

Day 10 **局部 refresh** 未严格 **裁到屏幕内** → 指针拖到 **右/下边界外** 时，dirty rect 仍含 **屏外坐标** → **VRAM 写错区 / 合成错乱**。

**改 `sheet_refreshsub`：**

| 步骤 | 行为 |
|------|------|
| 算 dirty 与 sheet 交集 | 与 Day 10 相同 |
| **再与屏幕 `[0,w)×[0,h)` 求交** | **屏外部分直接跳过** |

→ **画面外坐标安全** — 边界 clamp 是 GUI 引擎 **必做** 项。

---
