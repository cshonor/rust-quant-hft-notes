## ② 移动鼠标指针 · 边界与重绘

| 变量 | 含义 |
|------|------|
| **`mx`, `my`** | 当前指针 **屏幕坐标** |
| **`mdec.x`, `mdec.y`** | 本包 **位移** |

**新坐标** = 当前 + 位移；**`if` 钳制** 在 `[0, width-1]` / `[0, height-1]` — **不出屏**。

**绘制流程（每帧/每事件）：**

1. 用 **背景色** 覆盖 **旧位置** 指针（隐藏）
2. 在 **(mx, my)** **重画** 16×16 箭头（Day 5）

→ **鼠标可自由移动、可点** — **交互里程碑**。

#### 遗留问题 · 无图层

指针移到 **底部任务条** 时 **直接写 VRAM** → **破坏任务栏像素** — **没有叠加/合成**。  
**伏笔：** 后续章节 **图层处理** 再修。

**HFT：** 类似 **无双缓冲 UI 直接 blit** 的撕裂/覆盖问题 — 要有 **独立 surface 或 damage region**。

---
