## ② 定时器驱动 · 高速交替

Day 12–13 **PIT tick** 已就绪 → 设 **0.02 s（20 ms）** 超时定时器。

| 阶段 | 行为 |
|------|------|
| 初版 | **一次性** far-JMP：A → B（单向） |
| 本步 | 超时 → **A↔B 互 far-JMP** — **循环交替** |

**验证 task B 真在跑：** 独立 **窗口** + **递增计数** 显示 — 即使 A 的 `HariMain` 在忙，B 的数 **仍涨**。

```
时间 ──►  A ──20ms──► B ──20ms──► A ──20ms──► B …
              far-JMP      far-JMP
```

---
