## ② 究竟做了些什么 · CPU 只认开关

CPU **不理解**「数字」「图像」「文字」— 只识别 **开（ON）/ 关（OFF）** 电信号，即 **0 和 1** 组成的二进制序列。

我们在编辑器里输入的十六进制，就是在 **直接向 CPU 下发机器语言指令**：

```
你看到的 hello, world
    ↑ 显存/BIOS 字符输出
    ↑ 由若干条机器指令逐字节执行出来
    ↑ 最底层全是 0/1
```

→ 对照 [01-CSAPP Ch3 机器级表示](../../../../01-CSAPP-3rd/chapter-03-machine-level-programs/)

---
