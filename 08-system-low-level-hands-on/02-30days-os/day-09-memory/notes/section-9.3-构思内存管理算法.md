## ③ 构思内存管理算法

已知 **总可用内存** → 需要 **分配 / 释放**。

#### 位图 / 分配表法

| 做法 | 将内存切成 **固定小块**，每块 **0/1** 标记占用 |
|------|--------------------------------------------------|
| **问题** | 若物理内存 **3GB**，粒度 4KB → 状态表 **~768KB** **仅元数据** — **太浪费** |

```
3GB / 4KB = 786432 块 → 每块 1bit 也要 ~96KB+；更粗粒度则碎片多
位图随 RAM 线性涨 — 小 OS 不想要
```

---
