## ① type 命令 · 读取文件内容

Day 18 **`dir`** 只 **列目录** — 今天要 **读内容**（类似 Windows **`type`** / Linux **`cat`**）。

#### 从目录项到磁盘字节

| 步骤 | 说明 |
|------|------|
| 解析 **FAT12 目录项** | 取 **起始簇号 `clustno`** |
| **地址公式（原书）** | **`磁盘映像地址 = clustno × 512 + 0x003e00`** |
| 读取 | 先实现 **≤512 字节** 小文件 **连续读** → Console 打印 |

```
dir 项 → clustno → 线性地址 → 读入 buffer → putchar 循环
```

→ [Day 18 FAT12 根目录](../day-18-dir/)

---
