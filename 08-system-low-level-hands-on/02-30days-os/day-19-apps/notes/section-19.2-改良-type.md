## ② 改良 type · 换行与制表符

初版 **逐字节原样显示** → 含 **控制符** 的文件 **排版乱**。

| 字节 | 处理 |
|------|------|
| **`0x0A`** | 换行（LF） |
| **`0x0D`** | 回车（CR）— 常与 LF 配合 |
| **`0x09`** | **Tab** — 光标跳到下一制表位 |

**效果：** **`.asm` / 源文件`** 在 Console 里 **可读** — Shell 不只是 hex dump。

---
